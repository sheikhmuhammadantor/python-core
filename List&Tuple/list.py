# List Like JS Array;
# List:
num = [55, 63, 83, 47, 39, 23];

# Length:
len_num = len(num);
print(len_num);

# Indexing:
idx = num[5];
print(idx);

# Slicing:
sl = num[2:5];
Nsl = num[-6:-3];
print(sl);
print(Nsl);

# [M, u, h, a, m, m, a, d]
#  0  1  2  3  4  5  6  7 
# -8 -7 -6 -5 -4 -3 -2 -1

# List Functions: ...more;
# Same as JavaScript - Array Methods;
"""
List.append(35) # adds one element at the end;
List.sort() # sorts in ascending order;
List.sort(reverse=True) # sorts in descending order;
List.reverse() # reverses list;
List.insert(idx, el) # insert element at index;
List.remove(63) # removes first occurrence of element;
List.pop(idx) # removes element at idx;
"""
