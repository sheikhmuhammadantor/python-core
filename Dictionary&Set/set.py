# Python Set is like JavaScript Set;
# Set:
collection = {1, 2, 3, 4, 5, 4, 5, "Hello", "world", "Hello"};

print(collection); # unordered and unique value only;

# Empty Set:
emp_set = set();

# Methods:
"""
Set.add(el) # adds an element;
Set.remove(el) # removes the element;
Set.clear() # empties the set;
Set.pop() # removes a random value;
Set.union(set2) # combines both values & returns new;
Set.intersection(set2) # combines common values & returns new;
"""
